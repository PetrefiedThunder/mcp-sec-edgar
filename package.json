{
  "name": "mcp-sec-edgar",
  "version": "1.0.0",
  "description": "MCP server wrapping the SEC EDGAR API for AI agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mcp-sec-edgar": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["mcp", "sec", "edgar", "filings", "xbrl"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
